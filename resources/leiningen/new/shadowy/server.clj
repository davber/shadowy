(ns {{ns}}.server
  "This can where you add a CLI or a web server, etc."
  (:require [taoensso.timbre :as timbre]))

(defn -main
  "The main function..."
  []
  ;; TODO: add something cool, if you need anything outside the React app
  (timbre/info "Not doing much right now"))
